global:
  scrape_interval: 2s # Scrape frequently so we see changes instantly in the load test

scrape_configs:
  - job_name: 'url-shortener'
    static_configs:
      - targets: ['host.docker.internal:8000']
